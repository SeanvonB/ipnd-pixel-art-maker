var color;
var height;
var width;

// When size is submitted by the user:
// 1. Store submission in vars
// 2. Call makeGrid() and pass it the vars

function makeGrid() {
// Destroy any previous grid!
// Use nested loops to:
    // 1. Create [height] #pixelCanvas rows
        // 2. Create [width] #pixelCanvas columns per row
            // 3. Insert color function into column cells
}
